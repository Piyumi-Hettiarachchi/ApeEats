<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ApeEats">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IT20020880">
        <activity android:name=".ResDrawer"></activity>
        <activity android:name=".RestaurantSignin" />
        <activity android:name=".RestaurantSignup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResHome" />
        <activity android:name=".createAccount" />
        <activity android:name=".CustomerProfile" />
        <activity android:name=".EditProduct" />
        <activity android:name=".RestaurantProfile" />
        <activity android:name=".ProductList" />
        <activity android:name=".AddProduct" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>