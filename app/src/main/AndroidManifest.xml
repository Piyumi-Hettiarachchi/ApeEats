<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anjana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Anjana">
        <activity android:name=".Feedthnyou">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Cart"></activity>
        <activity android:name=".restaurantAcc" />
        <activity android:name=".customerAcc">

        </activity>
        <activity android:name=".First" />
        <activity android:name=".RegisterCustomer"></activity>
        <activity android:name=".Order" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>